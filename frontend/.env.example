# DocuMind Frontend — Environment Variables
# Copy to .env and fill in values. NEVER commit .env to git.

# Backend API base URL.
# Local Vite dev: leave empty so /api and /health use the development proxy.
# Vite preview or production: set the deployed API origin followed by /api/v1.
VITE_API_URL=
